# 1. Native on const
nat const BAD_NATIVE_CONST: I64 = 1

# 2. Native on mod
nat mod BadNativeMod
end

# 3. Native on use
nat use Path.To.Item

# 4. Native on trait
nat trait BadNativeTrait
end

# 5. Native on impl
nat impl BadNativeTrait for I64
end

pub fun main() I64
  return 0
end
