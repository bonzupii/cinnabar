# 1. Native on const
native const BAD_NATIVE_CONST: Int = 1

# 2. Native on mod
native mod BadNativeMod
end

# 3. Native on use
native use Path.To.Item

# 4. Native on trait
native trait BadNativeTrait
end

# 5. Native on impl
native impl BadNativeTrait for Int
end
