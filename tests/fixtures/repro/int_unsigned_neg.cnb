# [EXPECT REJECTION]
#! Unary negation `-` requires a signed type; applying it to an unsigned
#! integer is a compile error.  Both a const initializer and a return
#! expression are rejected below.
const NEG_U16: U16 = -1

fun neg_u8() U8
  return -1
end

fun main() I64
  return 0
end
