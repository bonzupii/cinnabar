#! % returns Result(I64, DivError); a non-zero divisor at runtime is Ok.
pub fun main() I64
  var x: I64 = 17
  var y: I64 = 5
  val rem = match x % y
    Ok(value) => value
    Err(DivByZero) => 0
  end
  if rem == 2
    return 42
  end
  return 41
end
