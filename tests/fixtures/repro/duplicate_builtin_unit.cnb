# [EXPECT REJECTION]
# duplicate_builtin_unit (bug-report d3): declaring a name the language
# reserves for a builtin primitive must be rejected with a diagnostic that
# names the builtin, never a bare 'duplicate symbol'.

pub type Unit
  x: I64
end

fun main() I64
  return 0
end
