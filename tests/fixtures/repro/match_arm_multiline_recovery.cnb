# [EXPECT REJECTION]
type Msg
  Ping
  Quit
end

fun log_step(code: I64) Unit
  return Unit
end

fun process(msg: Msg) Unit
  match msg
    Msg.Ping =>
      log_step(1)
      log_step(2)
    Msg.Quit => log_step(0)
  end
  return Unit
end

fun main() I64
  process(Msg.Ping)
  return 0
end
