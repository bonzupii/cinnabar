fun f(x: I64) I64
  return (x * 8) & 0xF
end

pub fun main() I64
  var pc: I64 = 0
  var running: Bool = true
  var count: I64 = 0

  while running
    val code = f(pc)
    if code == 0
      pc = 1
      continue
    end

    if code == 8
      count = count + 1
      if count >= 3
        running = false
        continue
      end
      pc = 0
      continue
    end

    running = false
  end

  return count
end
