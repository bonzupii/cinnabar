#! Division with a runtime zero divisor: the divisor is not statically
#! known, so the program compiles and the runtime returns Err(DivByZero)
#! instead of trapping.
pub fun main() Int
  var divisor: Int = 0
  match 100 / divisor
    Ok(value) => return 6
    Err(DivByZero) => return 7
  end
end
