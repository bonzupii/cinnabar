# [EXPECT REJECTION]
# duplicate_user_symbol (control): two user declarations of the same name
# must still be rejected as a duplicate symbol — the builtin-redeclaration
# diagnostic is reserved for collisions with the language's own builtins.

pub type Foo
  x: I64
end

pub type Foo
  y: I64
end

fun main() I64
  return 0
end
